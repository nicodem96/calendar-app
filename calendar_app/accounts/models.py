from django.db import models
from django.contrib.auth.models import AbstractUser

#modello utente non completo--da implementare eventuale login classico tramite email

class CustomUser(AbstractUser):
    pass